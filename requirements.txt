# ATS Resume Checker — combined backend + frontend dependencies.
#
# Install with:
#     pip install -r requirements.txt
#
# After installing, download the spaCy model the backend uses at startup:
#     python -m spacy download en_core_web_sm
#     python -m spacy download en_core_web_md



# ── Backend: web framework ──
fastapi>=0.110
uvicorn[standard]>=0.27
gunicorn>=21.2.0
pydantic>=2.6
python-multipart>=0.0.9      # required by FastAPI for File/Form uploads
python-dotenv>=1.0

# ── Backend: NLP / ML ──
spacy>=3.7, <4.0
sentence-transformers>=2.5
numpy>=1.26
torch==2.12.1+cpu
torchvision==0.27.1+cpu
--extra-index-url https://download.pytorch.org/whl/cpu
requests >= 2.31.0
rapidfuzz>=3.0.0

# ── Backend: file parsing ──
pdfplumber>=0.11
PyPDF2>=3.0
python-docx>=1.1
python-magic>=0.4.27; sys_platform != 'win32'
python-magic-bin>=0.4.14; sys_platform == 'win32'
python-dotenv>= 1.0.0
motor >= 3.3.0

# ── Backend: PDF report generation ──
jinja2>=3.1
weasyprint>=60.0
xhtml2pdf>=0.2.17
pypdf>=6.0.0

# ── Backend: external services ──
httpx>=0.27                  # async Supabase REST client
groq>=0.13                   # Groq LLM client (0.13+ is required for httpx>=0.28 compatibility)
PyJWT[crypto]>=2.8           # verifies Supabase access tokens (HS256)

# ── Frontend ──
streamlit>=1.32
requests>=2.31
supabase>=2.4                # frontend auth client (email/password + OAuth)